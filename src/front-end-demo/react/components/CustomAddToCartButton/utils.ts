export function showToast(message: string) {
  // Show a toast message for product added to cart
}

export async function updateShippingData(orderForm: any, setOrderForm: (newValue: any) => void) {
  // Update shipping data in order form. It's a long running task.
}

export async function regionalize(itemId: string, sellerId: string, setOrderForm: (newValue: any) => void) {
  // Ensure regionalization in order form. It's a long running task.
}
